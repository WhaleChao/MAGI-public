# RC643 runtime security overlay.
#
# Apply only to a copy of the immutable r59 runtime.  Download with --no-deps,
# verify the adjacent wheelhouse manifest, install offline, then require both
# pip check and the release-bound vulnerability receipt to pass.

aiohttp==3.14.3
click==8.3.3
cryptography==50.0.0
datasets==5.0.1
docling==2.94.0
docling-slim==2.94.0
httplib2==0.32.0
idna==3.15
lxml==6.1.0
mcp==1.28.1
msal==1.38.0
pillow==12.3.0
pip==26.2
pyasn1==0.6.4
pydantic-settings==2.14.2
PyJWT==2.13.0
pypdf==6.15.0
pytest==9.0.3
python-multipart==0.0.31
setuptools==83.0.0
soupsieve==2.8.4
starlette==1.3.1
torch==2.13.0
torchvision==0.28.0
urllib3==2.7.0

# Removed from the candidate runtime:
# - mariadb: unused Python connector; mysql-connector-python remains canonical.
# - PyPDF2: all imports migrated to pypdf.
# - weasyprint: advisory has no fixed release; the existing reportlab fallback
#   remains the supported PDF path and is covered by regression tests.
