{
  "name": "magi-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Internal MAGI/Paperclip mobile wrapper for Tailscale access",
  "scripts": {
    "sync": "capacitor sync",
    "open:android": "capacitor open android",
    "build:android": "capacitor sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
