{
  "name": "magi-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Internal MAGI/Paperclip mobile wrapper for Tailscale access",
  "scripts": {
    "prepare:config": "python3 ../scripts/configure_mobile_app.py",
    "sync": "npm run prepare:config && capacitor sync",
    "open:android": "npm run prepare:config && capacitor open android",
    "build:android": "npm run prepare:config && capacitor sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
