# ============================================================
# MAGI — Multi-Agent Governance Infrastructure
# Python Dependencies
# ============================================================
# Install:  pip install -r requirements.txt
# Optional: pip install -r requirements-optional.txt
# ============================================================

# -- Core (required for basic MAGI operation) --
flask>=3.0
Flask-Login>=0.6
Werkzeug>=3.0
python-dotenv>=1.0
requests>=2.31
aiohttp>=3.9
discord.py>=2.3
line-bot-sdk>=3.5
mysql-connector-python>=8.2
psutil>=5.9
flask-cors
